version: "3.9"

services:
  api:
    build: .
    container_name: small-express-api
    ports:
      - "3000:3000"
    environment:
      MONGODB_URL: mongodb://mongo:27017/smallExpressApp
      PORT: 3000
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: